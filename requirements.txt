fastapi==0.104.1
uvicorn[standard]==0.24.0
skyfield==1.46
python-dateutil==2.8.2
pydantic==2.5.0
```

## The Problem

Your current `requirements.txt` likely looks like this (WRONG):
```
```txt
fastapi==0.104.1
...
```
```

It should just be plain text with NO backticks, NO markdown.

## Also Notice in Your Logs

The Procfile is trying to run:
```
uvicorn app:app --host 0.0.0.0 --port $PORT
```

But your Python file is named `main.py`, so it should be:
```
uvicorn main:app --host 0.0.0.0 --port $PORT
```

## Quick Fix - Two Files to Update:

**`requirements.txt`** (plain text, no backticks):
```
fastapi==0.104.1
uvicorn[standard]==0.24.0
skyfield==1.46
python-dateutil==2.8.2
pydantic==2.5.0
```

**`Procfile`** (plain text, no backticks):
```
web: uvicorn main:app --host 0.0.0.0 --port $PORT
